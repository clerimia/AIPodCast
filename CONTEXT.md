# Podcast Studio

单用户 AI 播客工作间。一个工作间 = 一个节目，围绕"一集稿件"做写稿、改稿、审核、定稿与合成。

## Language

### 工作间与单集

**工作间 (Workspace)**:
一个节目，拥有自己的素材、节目纪要、说话人配置与单集；工作间之间互不串门。
_Avoid_: 项目、栏目

**单集 (Episode)**:
工作间里的一集播客，唯一拥有一份稿件（内容），可积累多版脚本。
_Avoid_: 期、集（口语）

### 内容与脚本

**稿件 (Manuscript)**:
写稿大师的工作产物：一集的内容稿，逐行（**稿件行 = 台词 + 说话人**）编辑，随时可改。满意后点「定稿」生成脚本。
_Avoid_: 工作稿、草稿、内容（泛指）

**脚本 (Script)**:
调音大师的工作对象：由「定稿」从稿件生成的一版内容快照 + 每行 TTS 配置（停顿 / 语气 / 语速 / 情感…）。定稿时用默认 TTS 参数，内容随后冻结；调音大师只改脚本行的 TTS 配置，合成读脚本。每版定稿 = 一版脚本。
_Avoid_: 成片、成品、tune 表

**稿件行 (Manuscript Line)**:
稿件的内容行（台词 + 说话人），活实体，随时可改可删。有唯一数据库 id（Agent 的 read/add/edit 用它）与行号 L001（既是序列号也是顺序）。删除是逻辑删除（deleted 标记），id 永不复用。
_Avoid_: 行（单称歧义）、row

**脚本行 (Script Line)**:
脚本的内容行：定稿那一刻从稿件行**拷贝**（顺序 / 说话人 / 文本）+ 每行自有 TTS 配置。内容冻结，调音大师只改 TTS 配置。脚本行有**独立 uuid**，**不引用稿件行**（无血缘指针）——稿件行之后怎么改、怎么删都不影响它；它是自足的物化快照。_拷贝不是冗余：脚本行本身就是"定稿当时内容的存档"（ADR-0001）。_
_Avoid_: 稿件行的副本、调音行、row

**音频缓存 (Audio Cache)**:
脚本行级单行音频的暂存：试听即生成并复用，批量合成命中已试听的行。缓存跟脚本行走、**与稿件无关**；破坏缓存是特定行为（改语气 / 语速 / 情感、强制重新生成、换音色 / 换模型）重写对应音频，改停顿只在拼接层、不重写。
_Avoid_: TTS 缓存（易与 TTS 引擎混淆）

### 配置与素材

**说话人 (Speaker)**:
工作间配置的声音角色（名称 + 音色）。行引用其 id。
_Avoid_: cast、声优

**素材 (Resource)**:
归属于工作间的可检索资料，切块后供 AI 检索引用；跨工作间用"复制到"。
_Avoid_: 资料、资源（"资源库"是页面名）

**节目元数据 (Show Metadata)**:
工作间的元数据：节目大纲、主题、创建时间，以及口吻、术语、禁词等风格约定；AI 写稿时遵守。
_Avoid_: 节目纪要、Show Note、纪要

### 动作

**定稿 (Finalize)**:
把当前稿件生成一版脚本（第 N 版，默认 TTS 参数）：读当前稿件未删行，逐行拷贝（顺序 / 说话人 / 文本）物化为脚本行，内容冻结。不冻结稿件，可再改再定稿。
_Avoid_: 存稿、提交（泛称）

**合成 (Synthesize)**:
把一份脚本生成产物（音频 + 行级文稿 + 节目简介）的确定性流水线。合成输入 = 脚本（内容 + 每行 TTS 配置）。
_Avoid_: 生成、渲染、TTS

**微调 (Tune)**:
调音大师对脚本逐行调整 TTS 配置（停顿 / 语气 / 语速 / 情感）。发生在合成后的试听环节，只作为合成输入，不冻结在脚本内容里；保存后重新合成生效。
_Avoid_: 音频设置、调音、精修

**产物 (Artifact)**:
独立于脚本的实体：脚本合成出的结果（master 音频、行级文稿、节目简介）。一份脚本最多一份产物，重新合成时替换。
_Avoid_: 成品、成片、output

**审核 (Audit)**:
Auditor 子代理对稿件或某版指定脚本的检查，隔离上下文、只用校验工具。
_Avoid_: 检查、把关

**修改 (Change)**:
稿件的一次原子变更（改字 / 插入 / 删除 / 重排）。用户发起的修改**先暂存**，确认（提交改动）后才持久化并追加一条消息进 AI 上下文；Agent 用工具发起的修改不追加，工具返回结果即 Agent 所见。
_Avoid_: ChangeSet、变更记录

### 会话与工具

按页面拆成两个平级 AI 会话，各占一层职责、各有各的中间产物，互不进入对方上下文：

**写稿大师 (Writer)（编辑页）**:
编辑页的 AI 会话，只管稿件（内容）：写稿、改稿、对齐都在这一个会话里。没有 tune 工具，够不到脚本的 TTS 配置。
_Avoid_: 主会话、编辑器

**调音大师 (Mixer)（脚本页）**:
脚本页的 AI 会话，**一版脚本一个会话、跟着点开的脚本走**：只有点选某版脚本后会话才出现，切换脚本即切换会话。只管脚本的听感：读脚本（内容 + TTS 配置）、逐行调 TTS 配置（tune），触发合成。没有 add / edit 工具，改不了稿件和脚本内容。
_Avoid_: 调音台、音效师

**read**:
工具（写稿大师）：读当前稿件（内容），直接返回当前行状态，不重放历史。

**add**:
工具（写稿大师）：在稿件中新增一行。

**edit**:
工具（写稿大师）：修改指定的行（改字、插入、删除、重排）。

**retrieve**:
工具（写稿大师）：检索本工作间知识库（素材），只翻本工作间。

**tune**:
工具（调音大师）：设置某版脚本某一行的话音参数（停顿 / 语气 / 语速 / 情感）；只作用于脚本行的 TTS 配置，不写稿件。

**Auditor**:
子代理：唯一另开隔离上下文的审核角色，审核稿件或指定脚本，仅有校验工具。与两个大师平级，不归属任何一方。
