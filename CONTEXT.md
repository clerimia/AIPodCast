# Podcast Studio

单用户 AI 播客工作间。一个工作间 = 一个节目，围绕"一集脚本"做写稿、改稿、审核与合成。

## Language

### 工作间与单集

**工作间 (Workspace)**:
一个节目，拥有自己的素材、节目纪要、说话人配置与单集；工作间之间互不串门。
_Avoid_: 项目、栏目

**单集 (Episode)**:
工作间里的一集播客，唯一拥有一份脚本（内容 + 每行指令），可生成一份产物。
_Avoid_: 期、集（口语）

### 内容与脚本（单层文本）

**脚本 (Script)**:
一集的内容本体：一组脚本行（说话人 + 台词 + 指令），活实体、随时可改。脚本 = 该集当前的全部文本，无版本历史，改了就改。合成读脚本，生成产物。
_Avoid_: 成片、成品、多版

**脚本行 (Script Line)**:
脚本的内容行：**说话人 + 台词 + 指令**。活实体，随时可改可删。有唯一数据库 id（Agent 的 read/add/edit 用它）与行号（既是序列号也是顺序）。删除是逻辑删除（deleted 标记），id 永不复用。合成时每行产出对应的**音频素材**。
_Avoid_: 调音行、row

**指令 (Instruction)**:
脚本行的自然语言"怎么说"描述（语气 / 情感 / 风格…），是文本的一部分，AI 生成、用户可改。合成时作为引擎的指令通道（qwen3-tts-instruct-flash 的 `input.instructions`），决定这行的听感。
_Avoid_: 提示词、tune 参数

### 音频与后期

**音频素材 (Audio Asset)**:
脚本行合成出的单行音频（clip）。跟随脚本行，**每行 0..1 份**，重合成时替换旧素材。素材是后期编辑的原子单位——携带文本 / 说话人 / 时长，可被后期拼接。
_Avoid_: TTS 缓存、音频缓存

**素材库 (Asset Library)**:
工作间级的共享音频容器，只装**可复用资产**（BGM / 音效 / 垫乐），跨集复用、不绑定单集。**不装对白素材**——对白素材跟随脚本行，是单集私有的（生命周期不同：对白随行合成替换，库资产长期存在）。
_Avoid_: 媒体库、音色库

**后期 (Post)**:
从脚本的音频素材（+ 素材库的垫乐）到产物的**确定性音频流水线**，不是 AI 会话：按序拼接素材、按停顿/语速参数插入间隔与变速、响度归一、回填行级时间戳。MVP 是参数化视图（列表 + 每行停顿/语速参数 + 默认间隔规则），时间线 UI 是增强。
_Avoid_: 调音台、渲染

### 配置与素材

**说话人 (Speaker)**:
工作间配置的声音角色（名称 + 音色）。脚本行引用其 id，合成时经说话人取音色。
_Avoid_: cast、声优

**素材 (Resource)**:
归属于工作间的可检索资料（文本），切块后供 AI 检索引用；跨工作间用"复制到"。
_Avoid_: 资料、音频素材（那叫素材库）

**节目元数据 (Show Metadata)**:
工作间的元数据：节目大纲、主题、创建时间，以及口吻、术语、禁词等风格约定；AI 写稿时遵守。
_Avoid_: 节目纪要、Show Note、纪要

### 动作

**合成 (Synthesize)**:
把一份脚本生成产物的确定性流水线：逐行取音频素材（命中复用，未命中调 TTS 合成并回填）→ 后期拼接（停顿 / 语速 / 响度 / 行级时间戳）→ 写产物。合成输入 = 脚本（内容 + 每行指令）。
_Avoid_: 生成、渲染、TTS

**产物 (Artifact)**:
独立于脚本的实体：脚本合成出的结果（master 音频、行级文稿、节目简介）。一份脚本最多一份产物，重新合成时替换。
_Avoid_: 成品、成片、output

**审核 (Audit)**:
Auditor 子代理对脚本（内容 + 指令）的检查，隔离上下文、只用校验工具。
_Avoid_: 检查、把关

**修改 (Change)**:
脚本的一次原子变更（改内容 / 改指令 / 插入 / 删除 / 重排）。用户发起的修改**先暂存**，确认（提交改动）后才持久化并追加一条消息进 AI 上下文；Agent 用工具发起的修改不追加，工具返回结果即 Agent 所见。改台词 / 改指令会触发该行音频素材重合成。
_Avoid_: ChangeSet、变更记录

### 会话与工具

**写稿大师 (Writer)（编辑页）**:
编辑页的 AI 会话，负责全部文本：写稿、改稿、生成/修改指令。工具面 read / add / edit / retrieve，**够不到音频素材和后期参数**（AI 不碰音频）。但**编辑页是用户的工作台**：上半区是文本编辑（写稿大师的产出落这里），下半区是音频工作区——用户在这里试听单行素材、触发合成、调停顿/语速参数、播放 master。AI 工具面不含音频 ≠ 用户界面不能碰音频。
_Avoid_: 主会话、编辑器

**后期流水线 (Post Pipeline)**:
不是 AI 会话，是编辑页下半区的音频工作区：脚本行列表 + 每行停顿/语速参数 + 默认间隔规则；用户在此试听单行（触发单行合成）、触发整集合成、产出 master。时间线 UI 是增强（同一素材序列模型）。

**read**:
工具（写稿大师）：读当前脚本（内容 + 指令），直接返回当前行状态，不重放历史。

**add**:
工具（写稿大师）：在脚本中新增一行。

**edit**:
工具（写稿大师）：修改指定的行（改字、改指令、插入、删除、重排）。

**retrieve**:
工具（写稿大师）：检索本工作间知识库（素材），只翻本工作间。

**Auditor**:
子代理：唯一另开隔离上下文的审核角色，审核脚本（内容 + 指令），仅有校验工具。与写稿大师平级。
